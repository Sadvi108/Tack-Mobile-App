# ---- client-safe (compiled into the Flutter bundle) ----
SUPABASE_URL=
SUPABASE_ANON_KEY=
APP_ENV=dev

# ---- server-only (Supabase Edge Function secrets — NEVER in the app) ----
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
GEMINI_API_KEY=
AI_PROVIDER=mock
CRON_SECRET=
